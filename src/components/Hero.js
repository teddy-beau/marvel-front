// import { useLocation } from "react-router-dom";
import banner from "../assets/images/avengers-banner.jpg";

const Hero = () => {
   return <img src={banner} alt="Hero banner" className="hero-banner" />;
};

export default Hero;
